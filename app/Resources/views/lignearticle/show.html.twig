{% extends 'base.html.twig' %}

{% block body %}
    <h1>Lignearticle</h1>

    <table>
        <tbody>
            <tr>
                <th>Quantite</th>
                <td>{{ lignearticle.quantite }}</td>
            </tr>
            <tr>
                <th>Idligne</th>
                <td>{{ lignearticle.idligne }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('lignearticle_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('lignearticle_edit', { 'id': lignearticle.idligne }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
