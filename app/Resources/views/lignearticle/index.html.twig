{% extends 'base.html.twig' %}

{% block body %}
    <h1>Lignearticle list</h1>

    <table>
        <thead>
            <tr>
                <th>Quantite</th>
                <th>Idligne</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for lignearticle in lignearticles %}
            <tr>
                <td><a href="{{ path('lignearticle_show', { 'id': lignearticle.idligne }) }}">{{ lignearticle.quantite }}</a></td>
                <td>{{ lignearticle.idligne }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('lignearticle_show', { 'id': lignearticle.idligne }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('lignearticle_edit', { 'id': lignearticle.idligne }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('lignearticle_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
